<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SnookerScore" parent="android:Theme.Material.Light.NoActionBar">

        <item name="android:background">@color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.SnookerScore.Splash" parent="Theme.SplashScreen.IconBackground">

        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="windowSplashScreenBackground">@color/background</item>
        <item name="postSplashScreenTheme">@style/Theme.SnookerScore</item>
    </style>

</resources>